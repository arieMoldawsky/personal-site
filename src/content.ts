// The whole page, in a few words. Hints only — nothing that reads like a CV.

export const name = 'Arie Moldawsky'

// Cycles under the name, one at a time.
export const verbs = ['drafting', 'compiling', 'structuring', 'building']

// Tiny corner marks.
export const corners = {
  topLeft: 'A—M',
  topRight: 'v0.1',
  bottomRight: '§ 0.0',
}
